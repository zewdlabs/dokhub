{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": [".next/**", "!.next/cache/**"]
    },
    "lint": {
      "dependsOn": ["^lint"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "db:migrate:deploy": {},
    "db:migrate:dev": {},
    "db:push": {},
    "db:seed": {
      "dependsOn": ["^db:seed"]
    },
    "generate": {
      "dependsOn": ["^generate"]
    },
    "db:studio": {
      "dependsOn": ["^db:studio"]
    }
  }
}
